fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
torch>=2.3
python-chess>=1.11
numpy>=1.26
huggingface_hub>=0.23
# The full RL/serving package (encoding, MCTS, continual trainer, server).
# Pinned to a commit so a Space rebuild always installs the intended code and
# busts the Docker/pip cache (bump this hash when the package changes).
shannons-gambit @ git+https://github.com/aravinds-kannappan/Chess-Gambit-RL.git@46a0be271563e2f25565ff4a248d6538155e4f88
