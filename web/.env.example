# Optional: a Hugging Face Inference Endpoint serving the trained model.
# When set, the API routes proxy to it; otherwise the built-in TypeScript
# fallback agent (chess.js heuristic) is used so the site always works.
HF_ENDPOINT_URL=
HF_API_TOKEN=
HF_MODEL_ID=aravindk/shannons-gambit
